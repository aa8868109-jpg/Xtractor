# تقرير إعادة الفحص الأمني للموقع

**النطاق:** `https://xtractor-smart-attendance-system.vercel.app`  
**تاريخ الفحص:** 25 سبتمبر 2026  
**نوع الفحص:** فحص خارجي غير تدميري، شمل القراءة والتحقق من الاستجابات فقط، ولم يتضمن تعديل سجلات أو استخدام بيانات دخول حقيقية.  
**المُعدّ:** Manus AI

## الخلاصة التنفيذية

أظهرت إعادة الفحص تحسناً واضحاً مقارنة بالفحص السابق: لم تعد مفاتيح Airtable ظاهرة في الصفحة أو في `app.min.js`، كما أضيفت عدة رؤوس أمنية مهمة، منها `X-Frame-Options: DENY` و`X-Content-Type-Options: nosniff` و`Referrer-Policy` و`Permissions-Policy`.

لكن ما تزال هناك **مشكلة عالية الخطورة**: نقاط البيانات الخلفية تسمح بالقراءة بدون مصادقة، بما في ذلك `/api/data/LEC_1`. أثناء الفحص أعاد هذا المسار 40 سجلاً وحقولاً تتضمن رمز الطالب، الاسم، عنوان الموقع الجغرافي، عنوان IP، وقت الحضور، وحالات مسح رموز QR. هذا يمثل كشفاً مباشراً لبيانات حضور وموقع طلاب، حتى لو لم تظهر القيم نفسها في هذا التقرير.

كما توجد ملاحظة أمنية مهمة في الواجهة: التطبيق يستخدم `innerHTML` لبناء قائمة الطلاب اعتماداً على قيم قادمة من البيانات، ولم يتم إثبات تنفيذ XSS خلال هذا الفحص، لكن المسار يستحق المعالجة الوقائية لأن إدخال قيمة HTML في أحد الحقول قد يؤدي إلى XSS مخزنة أو منعكسة إذا لم تكن القيم مُنقّاة على الخادم والعميل.

## ملخص النتائج

| المعرّف | النتيجة | الخطورة | الحالة الحالية |
|---|---|---:|---|
| R-01 | قراءة سجلات الحضور بدون مصادقة | عالية | مؤكدة |
| R-02 | كشف بيانات حساسة: Student Code وLocation وDevice IP وTime | عالية | مؤكدة ضمن R-01 |
| R-03 | API المصادقة يرفض الكود العشوائي | إيجابي | تم التحقق منه |
| R-04 | مفاتيح Airtable غير موجودة في الأصول العامة الحالية | إيجابي | يبدو مُعالَجاً |
| R-05 | رؤوس أمنية أساسية مفعّلة | إيجابي | تم التحسين |
| R-06 | غياب Content-Security-Policy | متوسطة | ما تزال قائمة |
| R-07 | استخدام innerHTML مع بيانات سجلات الطلاب | متوسطة/محتملة | تحتاج مراجعة وإصلاح وقائي |

## النتائج التفصيلية

### R-01 وR-02 — نقاط بيانات عامة تكشف سجلات الحضور والموقع

تم طلب المسارات التالية بدون رمز وصول أو جلسة تسجيل دخول:

| المسار | النتيجة |
|---|---|
| `/api/data/MODE` | HTTP 200، سجل واحد |
| `/api/data/LEC_1` | HTTP 200، عدد 40 سجلاً |
| `/api/data/LEC_20` | HTTP 200، لا توجد سجلات |

استجابة `/api/data/LEC_1` تضمنت سجلات تحتوي على الحقول التالية: `Code`، و`name`، و`Location`، و`Device IP`، و`Time`، و`Region`، و`1st QR`، و`2nd QR`، و`3rd QR`. هذه ليست مجرد بيانات إعدادات عامة؛ بل تتضمن معرفات طلاب ومعلومات حضور وموقع جغرافي وعنوان شبكة.

**الأثر:** يستطيع أي مستخدم على الإنترنت قراءة بيانات الحضور من خلال طلبات HTTP مباشرة، دون الحاجة إلى تسجيل دخول. وقد يؤدي ذلك إلى انتهاك خصوصية الطلاب، وتتبع المواقع، وكشف عناوين الشبكة، واستخدام رموز الطلاب في محاولات دخول لاحقة.

**الإصلاح العاجل المقترح:** يجب أن يتحقق الخادم من هوية وصلاحية كل طلب قبل تنفيذ `/api/data/*`. لا يكفي إخفاء المسار أو الاعتماد على JavaScript في المتصفح. استخدم جلسة خادمية أو JWT قصير العمر مع صلاحيات منفصلة للطالب والطبيب، وطبّق مبدأ أقل صلاحية. يجب ألا يعيد مسار الطالب إلا السجل المسموح به لذلك الطالب، بينما يقتصر مسار الطبيب على سجلات المحاضرات المسموح بها. كما يجب إزالة `Device IP` و`Location` من الاستجابات التي لا تحتاج إليها، وتطبيق pagination وrate limiting وتسجيل عمليات الوصول.

### R-03 — اختبار المصادقة السلبية

أُرسل كود عشوائي غير صالح مرة واحدة إلى `/api/auth`. أعاد الخادم `HTTP 401` مع `authenticated: false`، لذلك لم يظهر تجاوز دخول مباشر في هذا الاختبار المحدود. هذه نتيجة إيجابية، لكنها لا تثبت سلامة نظام الصلاحيات الخاص بمسارات البيانات؛ فالمشكلة الأساسية هي أن مسارات البيانات نفسها أعادت بيانات بدون مصادقة.

### R-04 — مفاتيح Airtable المكشوفة

في الفحص السابق كانت مفاتيح Airtable موجودة داخل JavaScript. في النسخة الحالية، لم تظهر مفاتيح من نمط Airtable PAT أو معرفات القواعد داخل HTML أو `app.min.js`. كما أن التطبيق الحالي يستدعي مسارات محلية مثل `/api/auth` و`/api/data/...` بدلاً من وضع مفاتيح Airtable في المتصفح.

**الحكم:** يبدو أن هذه الثغرة عولجت في النسخة الحالية. مع ذلك، يجب إبطال المفاتيح القديمة وإعادة إصدارها إن لم يتم ذلك، لأن المفاتيح التي نُشرت سابقاً يجب اعتبارها مكشوفة نهائياً. يجب كذلك مراجعة سجل Airtable للتأكد من عدم وجود تعديلات غير معروفة.

### R-05 — رؤوس الأمان الحالية

الرؤوس التالية ظهرت مفعّلة في استجابة الموقع الرئيسية:

| الرأس | القيمة أو الحالة |
|---|---|
| `Strict-Transport-Security` | `max-age=31536000; includeSubDomains` |
| `X-Content-Type-Options` | `nosniff` |
| `X-Frame-Options` | `DENY` |
| `Referrer-Policy` | `strict-origin-when-cross-origin` |
| `Permissions-Policy` | `camera=(self), geolocation=(self), microphone=()` |

هذه تحسينات مهمة، وخصوصاً `X-Frame-Options: DENY` الذي يقلل خطر Clickjacking. لم يظهر `Content-Security-Policy`، كما لم تظهر رؤوس Cross-Origin Isolation مثل `Cross-Origin-Opener-Policy`، لكن غياب الأخيرة ليس بالضرورة ثغرة مباشرة لهذا التطبيق.

### R-06 — غياب Content-Security-Policy

لم يظهر رأس `Content-Security-Policy` في الاستجابة. وبما أن الصفحة تحمل مكتبات JavaScript من cdnjs وjsDelivr وملفات خطوط من Google، فإن تطبيق CSP يحتاج إلى إعداد تدريجي واختبار دقيق حتى لا تتعطل الوظائف.

**الإصلاح المقترح:** ابدأ بـ `Content-Security-Policy-Report-Only`، ثم انتقل إلى سياسة تنفيذية بعد مراجعة التقارير. تجنب `unsafe-inline` و`unsafe-eval` قدر الإمكان، واستخدم nonces أو hashes للسكربتات المضمنة. يجب أيضاً تثبيت إصدارات المكتبات الخارجية واستخدام Subresource Integrity حيثما أمكن.

### R-07 — استخدام innerHTML مع بيانات الطلاب

يستخدم `app.min.js` `innerHTML` لبناء عناصر قائمة الطلاب، ويتضمن القالب قيماً مثل اسم الطالب ورمز الطالب قادمة من سجلات البيانات. لم أقم بحقن حمولة XSS أو تعديل أي سجل، لذلك لا أُصنّف هذا كاستغلال مؤكد. لكنه مسار خطر يجب إصلاحه وقائياً.

**الإصلاح المقترح:** استخدم `textContent` للعناصر النصية، أو أنشئ العناصر عبر DOM APIs، أو طبّق ترميز HTML موثوقاً قبل الإدراج. والأهم أن يفرض الخادم قيوداً على شكل وطول الحقول، ويرفض وسوم HTML في الاسم والرمز، مع تسجيل ومحاصرة أي قيمة غير متوقعة.

## ترتيب الإصلاح المقترح

أولاً، أغلق الوصول العام إلى `/api/data/*` فوراً، ثم اختبر ذلك بطلب بدون جلسة وتأكد من حصوله على `401` أو `403`. ثانياً، طبّق التفويض على مستوى السجل والحقول بحيث لا يستطيع الطالب قراءة سجلات الآخرين ولا يستطيع أي مستخدم تعديل سجلات لا تخصه. ثالثاً، اعتبر مفاتيح Airtable القديمة مُخترقة وأبطلها، وراجع سجل التغييرات. رابعاً، عالج استخدام `innerHTML`، ثم أضف CSP تدريجية عبر `Report-Only` قبل تفعيلها. وأخيراً، أضف اختبارات آلية تمنع إعادة نشر API يعيد بيانات الحضور بدون مصادقة.

## حدود الفحص

هذا الفحص خارجي وغير تدميري. لم يتم اختبار كلمات مرور حقيقية، ولم يتم تعديل أو حذف سجلات، ولم يتم تنفيذ حمولة XSS، ولم يتم إجراء تخمين واسع للمسارات. لذلك ينبغي اعتبار نتيجة R-07 بحاجة إلى مراجعة كود واختبار داخلي إضافي، بينما نتيجة R-01/R-02 مؤكدة من خلال قراءة HTTP مباشرة للمسار العام.

## المراجع

[1]: https://xtractor-smart-attendance-system.vercel.app/ "الصفحة الرئيسية للموقع — تم فحصها في 25 سبتمبر 2026"
[2]: https://xtractor-smart-attendance-system.vercel.app/api/data/LEC_1 "نقطة بيانات LEC_1 — أعادت 40 سجلاً بدون مصادقة أثناء الفحص"
[3]: https://xtractor-smart-attendance-system.vercel.app/api/data/MODE "نقطة بيانات MODE — أعادت سجلاً بدون مصادقة أثناء الفحص"
[4]: https://xtractor-smart-attendance-system.vercel.app/app.min.js "ملف JavaScript العام الحالي"
