# Copy this file to `.env` and fill values. Keep `.env` out of version control.
# Do NOT commit real API keys. Use Vercel environment variables for production.
AIRTABLE_API_KEY=REPLACE_WITH_AIRTABLE_API_KEY
PROTECTION_API_KEY=REPLACE_WITH_PROTECTION_API_KEY
PROTECTION_BASE_ID=REPLACE_WITH_PROTECTION_BASE_ID
PROTECTION_TABLE=Protection
PORT=3000

# Notes:
# - For local development, create a `.env` from this file and populate real values.
# - Ensure `.env` is listed in `.gitignore` so secrets are not committed.